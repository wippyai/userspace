version: "1.0"
namespace: app.env

entries:
  # app.env:upload_storage_id
  - name: upload_storage_id
    kind: env.variable
    meta:
      comment: Upload storage service ID for file upload handling
      icon: tabler:upload
      private: true
    default: app:uploads
    storage: app.env:router
    variable: UPLOAD_STORAGE_ID