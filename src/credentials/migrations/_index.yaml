version: "1.0"
namespace: userspace.credentials.migrations

entries:
  # userspace.credentials.migrations:01_create_credentials_tables
  - name: 01_create_credentials_tables
    kind: function.lua
    meta:
      type: migration
      tags:
        - credentials
        - database
        - encryption
      description: Create credentials_store table for encrypted credential storage
      target_db: app:db
      timestamp: "2025-06-22T10:00:00Z"
    source: file://01_create_credentials_tables.lua
    imports:
      migration: wippy.migration:migration
    method: migrate
    