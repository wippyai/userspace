version: "1.0"
namespace: userspace.user.traits

entries:
  # userspace.user.traits:user_aware
  - name: user_aware
    kind: registry.entry
    meta:
      type: agent.trait
      title: User Aware
      comment: Inject current username into prompt
      tags:
        - user
        - context
      icon: tabler:user
    prompt: User context awareness.
    prompt_func_id: userspace.user.traits:user_name
    
  # userspace.user.traits:user_name
  - name: user_name
    kind: function.lua
    meta:
      comment: Get current user name for prompt injection
    source: file://user_name.lua
    modules:
      - security
    method: handler
    