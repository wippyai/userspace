version: "1.0"
namespace: userspace.oauth.migrations

entries:
  # userspace.oauth.migrations:01_create_oauth_connections_table
  - name: 01_create_oauth_connections_table
    kind: function.lua
    meta:
      type: migration
      tags:
        - oauth
        - connections
        - database
        - tokens
      description: Create oauth_connections table for OAuth connection and token storage
      target_db: app:db
      timestamp: "2025-06-22T12:00:00Z"
    source: file://01_create_oauth_connections_table.lua
    imports:
      migration: wippy.migration:migration
    method: migrate
