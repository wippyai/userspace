version: "1.0"
namespace: userspace.mcp.service

entries:
  # userspace.mcp.service:exec
  - name: exec
    kind: process.lua
    meta:
      comment: MCP server process manager for exec/stdio mode
      tags:
        - mcp
        - server
        - exec
    source: file://exec.lua
    modules:
      - time
      - logger
      - exec
      - json
    imports:
      mcp_consts: userspace.mcp:consts
    method: run

  # userspace.mcp.service:http
  - name: http
    kind: process.lua
    meta:
      comment: MCP server manager for streamable-HTTP transport
      tags:
        - mcp
        - server
        - http
    source: file://http.lua
    modules:
      - logger
      - http_client
      - json
    imports:
      mcp_consts: userspace.mcp:consts
    method: run
